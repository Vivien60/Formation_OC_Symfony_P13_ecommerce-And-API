<article class="product-card" data-component="card">
    <div class="product-card__image">
        <img src="{{ asset('images/products/default.png') }}" alt="">
    </div>
    <div class="product-card__info">
        <header class="product-card__heading">
            <h2 class="product-card__title">{{ name }}</h2>
            <p class="product-card__tagline">{{ tagline }}</p>
        </header>
        <p class="product-card__price">{{ price }}</p>
        <a class="product-card__cta card__link" type="button" href="{{ path('app_product_show', {'id' : id}) }}">Voir le produit</a>
    </div>
</article>
